//--------------------------------------------------------------------------------------------------
// An blinking example script
//--------------------------------------------------------------------------------------------------
// led pin-out
const led = 25;
// set output mode
pinMode(led, OUTPUT);
// toggle led state in interval 1s
setInterval(() => {
    digitalToggle(led);
}, 1000);
//--------------------------------------------------------------------------------------------------